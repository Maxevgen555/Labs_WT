@page "/"

<PageTitle>🍽️ Ресторан "Вкусно"</PageTitle>

<div class="container py-4">
    <!-- Главный заголовок -->
    <div class="text-center mb-5">
        <h1 class="display-4 fw-bold text-primary mb-3">
            <i class="bi bi-restaurant me-3"></i>Добро пожаловать
        </h1>
        <p class="lead fs-4 text-muted">
            В ресторан, где каждый прием пищи становится праздником
        </p>
    </div>

    <!-- Карточки преимуществ -->
    <div class="row g-4 mb-5">
        <div class="col-md-4">
            <div class="card h-100 border-0 shadow-sm">
                <div class="card-body text-center p-4">
                    <i class="bi bi-egg-fried display-3 text-warning mb-3"></i>
                    <h4 class="card-title">Свежие ингредиенты</h4>
                    <p class="card-text text-muted">Используем только качественные продукты от местных фермеров</p>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card h-100 border-0 shadow-sm">
                <div class="card-body text-center p-4">
                    <i class="bi bi-fire display-3 text-danger mb-3"></i>
                    <h4 class="card-title">Контроль калорий</h4>
                    <p class="card-text text-muted">Каждое блюдо имеет точный расчет пищевой ценности</p>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card h-100 border-0 shadow-sm">
                <div class="card-body text-center p-4">
                    <i class="bi bi-clock-history display-3 text-success mb-3"></i>
                    <h4 class="card-title">Быстрое обслуживание</h4>
                    <p class="card-text text-muted">Приготовление и доставка в кратчайшие сроки</p>
                </div>
            </div>
        </div>
    </div>

    <!-- Кнопка перехода в меню -->
    <div class="text-center">
        <a href="/products" class="btn btn-primary btn-lg px-5 py-3">
            <i class="bi bi-menu-button-wide me-2"></i>Перейти к меню
        </a>
    </div>
</div>

<style>
    .card {
        transition: transform 0.2s;
    }

        .card:hover {
            transform: translateY(-5px);
        }

    .btn-primary {
        background: linear-gradient(45deg, #0d6efd, #6610f2);
        border: none;
        transition: all 0.3s;
    }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(13, 110, 253, 0.3);
        }
</style>